import React from 'react';

import { HeaderStyled } from './index.style';

export const Header = (): JSX.Element => {
    return (
        <HeaderStyled>
            {'Header'}
        </HeaderStyled>
    );
};
