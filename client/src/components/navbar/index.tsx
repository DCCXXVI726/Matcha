import React from 'react';

import { NavbarStyled } from './index.style';

export const Navbar = (): JSX.Element => {
    return (
        <NavbarStyled>
            {'Navbar'}
        </NavbarStyled>
    );
};
