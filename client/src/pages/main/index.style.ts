import styled from '@emotion/styled';

export const MainStyled = styled.main`
    display: grid;
    align-items: center;
    max-width: unset;
    width: 100%;
    padding: 20px 0px;
    user-select: none;
    grid-area: main;
`;
