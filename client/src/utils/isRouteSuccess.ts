import { Redirect } from 'react-router';
import { SUCCESS } from '../__data__/constants';

export const isRouteSuccess = () => {
    if (status === SUCCESS && ) {
        return true;
    }
    return false;
}
